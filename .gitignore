# --- License-restricted (competition-use-only, DO NOT COMMIT) ---
# cabt engine bindings + libcg.so/.dll/.dylib
/cg/
# card CSVs / PDFs
/data/
*.zip
submission.tar.gz

# --- Local dev artifacts ---
# generated match traces (eval/trace.py) — reproducible, not source
/eval/traces/
# arena run artifacts (eval/arena.py: manifest/results/report/traces) — reproducible, not source
/eval/arena_runs/
# deck-track run artifacts (eval/deck_eval.py: report.json) — reproducible, not source
/eval/deck_runs/
venv/
__pycache__/
*.pyc
.env
.DS_Store
# self-play PPO training data (eval/selfplay.py: JSONL) — regenerable, not source
/eval/selfplay_runs/
