# --- License-restricted (competition-use-only, DO NOT COMMIT) ---
# cabt engine bindings + libcg.so/.dll/.dylib
/cg/
# card CSVs / PDFs
/data/
*.zip
submission.tar.gz

# --- Local dev artifacts ---
# generated match traces (eval/trace.py) — reproducible, not source
/eval/traces/
# arena run artifacts (eval/arena.py: manifest/results/report/traces) — reproducible, not source
/eval/arena_runs/
venv/
__pycache__/
*.pyc
.env
.DS_Store
